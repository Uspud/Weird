#include <iostream>

using namespace std;

int n, cnt;

int dx[4] = {0, 1, 0, -1};
int dy[4] = {-1, 0, 1, 0};

int map[25][25];
int visited[25][25];

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n;

    return 0;
}