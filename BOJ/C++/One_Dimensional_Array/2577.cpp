#include <iostream>

using namespace std;

int main(void) {
    int a, b, c, algebra[10];

    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    cin >> a;
    cin >> b;
    cin >> c;

    int sum = a*b*c;

    for (int i = 0; i < 10; i++) {
        
    }
}