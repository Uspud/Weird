#include <iostream>

using namespace std;

int main(void) {
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    char s[1000001];
    int alph[26] = { 0, };

    cin >> s;

    for(int i = 0; i < 1000001; i++) {
        if(s[i] == '\0') break;

        
    }
}